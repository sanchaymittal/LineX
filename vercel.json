{
  "buildCommand": "tsc && mkdir -p api && cp -r dist/* api/",
  "outputDirectory": "",
  "functions": {
    "api/index.js": {
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}